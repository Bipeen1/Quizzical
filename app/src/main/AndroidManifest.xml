<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizzical">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizzical">
        <activity android:name=".QuizActivity"></activity>
        <activity android:name=".DialogueActivity" />
        <activity android:name=".QuestionList" />
        <activity android:name=".SubjectListActivity" />
        <activity android:name=".QuestionToBeAdded" />
        <activity android:name=".SubjectToBeAdd" />
        <activity android:name=".BaseActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.Quizzical.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.Quizzical.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>